class Person:
  def __init__(self, enabled, url, key, authType, token, username, password, securityKey):
    self.enabled = enabled
    self.url = url
    self.key = key
    self.authType = authType
    self.token = token
    self.username = username
    self.password = password
    self.securityKey = securityKey